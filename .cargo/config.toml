[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# Make `cargo run` start a GDB session
runner = "arm-none-eabi-gdb -q -x openocd.gdb"

[build]
# Set the default compilation target to Cortex-M0+
target = "thumbv6m-none-eabi"
